version: '3.8'

services:
  movies:
    build:
      context: ./movies
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - redis
    environment:
      - PORT=3000
      - MONGODB_APP_ID=application-mflix-yqrpksi
      - REDIS_URL=redis://redis:6379
    networks:
      - cache-tier

  redis:
    image: redis:alpine    
    networks:
      - cache-tier

networks:
  cache-tier:
    driver: bridge
